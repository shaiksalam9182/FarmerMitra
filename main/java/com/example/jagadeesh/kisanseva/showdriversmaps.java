package com.example.jagadeesh.kisanseva;

/*
import android.app.Activity;
import android.os.Bundle;
import android.widget.Toast;

import com.google.android.gms.maps.GoogleMap;
import com.google.android.gms.maps.MapFragment;
import com.google.android.gms.maps.OnMapReadyCallback;
import com.google.android.gms.maps.model.LatLng;
import com.google.android.gms.maps.model.MarkerOptions;

/**
 * Created by salam on 23/12/16.
 */


import android.app.Activity;
import android.os.Bundle;

public class showdriversmaps extends Activity /*implements OnMapReadyCallback*/{

    double lat,lon;
    String drie;
    double latt=16;
    double lattt=18;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.showdrivermaps);}}


        /*Bundle gotbasket = getIntent().getExtras();
        lat= gotbasket.getDouble("latti");
        lon = gotbasket.getDouble("lundi");
        drie = gotbasket.getString("name");
        Toast.makeText(showdriversmaps.this,lat+"\n"+lon,Toast.LENGTH_LONG).show();

        MapFragment Mm = (MapFragment) getFragmentManager().
                findFragmentById(R.id.xxmap);
        Mm.getMapAsync(this);

    }

    @Override
    public void onMapReady(GoogleMap googleMap) {

        googleMap.addMarker(new MarkerOptions()
                .position(new LatLng(lat,lon))
                .title("salam"));

    }
}
*/
